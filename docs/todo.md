# Mines of Idle Doomath — UX, Improvements & New Features Plan

Legend: [ ] not started, [o] in progress
Completed items are removed from this file (see git history); only remaining work is tracked here.

- [o] Rewarded ads (AdMob) — production ids + on-device verification (integration itself is done; the remaining half is external — see `docs/blockers.md`)
  - [x] **External (done):** Android AdMob App ID + the production combo-save rewarded unit landed in `storeConfig.adMob` (unit ids are keyed per placement — `AdKind` — per platform; the other three placements run AdMob's public test unit ids until their production units are created). App IDs also in the `adMobAppIds` block in `app.config.ts` (pinned together by `storeConfig.test.ts`).
  - [ ] **External:** create the remaining rewarded ad units (gem rolls, offline double, offline top-up — one per placement; AdMob units aren't platform-scoped so one set serves both platforms) + the iOS app entry → fill the test-id slots in `storeConfig.adMob.rewardedUnitAndroid/Ios` + `iosAppId` **and** the `adMobAppIds` block in `app.config.ts` → `npx expo prebuild` → verify on device per `docs/store-integration.md` §1/§3.

- [ ] IAP — Pocketbase deploy + store products + on-device verification (the client provider is done: `iapProvider.ts` / `iapDeviceId.ts` + the `selectIapProvider` swap + tests; the **server** is done too: `pb_hooks/` — all 8 endpoints verified against a live Pocketbase v0.40.2 sandbox, fake-token mode, see `pb_hooks/README.md`; remaining work is external — see `docs/blockers.md`)
  - [ ] **External:** deploy `pb_hooks/` to a Pocketbase **v0.40.x** instance per `docs/pocketbase-plan.md` (sandbox first with `MDOOM_DEV_FAKE_TOKEN=1`; the folder is self-contained — no console clicking), then paste the URL into `storeConfig.pocketbaseUrl`
  - [ ] **External:** create the four products per the table in `docs/store-integration.md` §2 (exact `storeId`s) + the Play service-account credentials (server-side only)
  - [ ] **Decision:** real store-token verification needs RSA/ECDSA signing, which the Pocketbase JS runtime can't do — see the new signing-gap section in `docs/blockers.md` (options: tiny signing sidecar / move the two store round-trips / re-host)
  - [ ] On-device verification per `docs/store-integration.md` §3 (test purchase, restore on a wiped local key, web bundle grep incl. `expo-iap` + the Pocketbase URL)

- [ ] App store integrations
  - [o] Store integrations: cloud saves, leaderboard, achievements — designed in `docs/store-integration-plan.md` (device-keyed, no account; extends the same Pocketbase deployment as IAP)
    - [x] Server: the six cloud/leaderboard/GDPR endpoints in `pb_hooks/` (same deployment as the IAP ones) — done this iteration: last-write-wins push, monotonic leaderboard merge + top/rank, durable 30-write/hour budget, GDPR delete with entitlements surviving; verified live against Pocketbase v0.40.2 (fake-token sandbox)
    - [ ] **External:** same Pocketbase deploy as the IAP item above (sandbox first)
    - [ ] Identity decision pending — device-scoped (no account, plan's default) vs. login: see `docs/blockers.md`
    - [x] Client: cloud-save engine wiring — push cadence (autosave/prestige), launch-recovery path (corrupt local → cloud import), settings UI (toggle, last-sync status, manual restore) + tests. Done: `hooks/useCloudSave.ts` (5-min push gate, prestige bypass, recovery only on a FAILED local load, persisted last-sync status, manual restore) + `useGameEngine.restoreFromBlob`/`saveLoadFailed` (restore goes through the normal validate/migrate/offline-pay pipeline) + the new settings section in `SettingsPanel` (hides while the provider is unavailable; the dev sim is labeled) + tests (`useCloudSave.test.ts`, `useGameEngine.test.ts`).
    - [x] Client: leaderboard — monotonic submit piggyback (dirty→clean + prestige save flushes, 5-min cadence in the hook), top-10 bottom-sheet + trophy button (hidden until the provider is available — the availability gate), ≤16-char display name (persisted in AsyncStorage, never in the save), 60s board cache + 5s tap throttle, "you" row / "not ranked" line. Done: `leaderboard.ts` (provider core: noop / labeled dev-sim / store, same never-reject fetch contract as `cloudSave.ts`, `pickLeaderboardProvider` mirrors `pickCloudSaveProvider`) + `hooks/useLeaderboard.ts` + `components/LeaderboardPanel.tsx` (+ a new optional `BottomModal` `onToggle` prop for refresh-on-open) + `MinesOfDoom` wiring (submit piggybacks the cloud-push triggers; only derived lifetime stats leave the device) + tests (`leaderboard.test.ts`, `useLeaderboard.test.ts`).
    - [x] Client: achievement share (📤 on each COMPLETED row → the platform share sheet; react-native `Share` on native, Web Share API on web, quiet no-op where neither exists — `share.ts` + `share.test.ts`; the badge count on board rows is server-side: submits already carry `achievementIds`) + "delete my data" GDPR button (a `ConfirmableButton` in the cloud-save settings section → `useCloudSave.deleteMyData` → `POST /api/app/delete`; button + hook + tests done, the endpoint itself is the External item above)
    - [ ] On-device verification per the plan §Phases 6

