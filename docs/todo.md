# Mines of Idle Doomath — UX, Improvements & New Features Plan

Legend: [ ] not started, [o] in progress, [-] blocked
Completed items are removed from this file (see git history); only remaining work is tracked here.
Only work on continuous tasks after other tasks are completed

- [ ] finish the `packSkin` release (the in-repo half LANDED 2026-09-10 — custom skinning one-time purchase, catalog row `pack_skin`, web picker, grant path, i18n — gates green; see git history): (1) ~~run `node scripts/stripe/syncStripe.mjs products` and paste the real test price over the `price_PENDINGPACKSKIN` placeholder in storeConfig.ts~~ **DONE 2026-09-10** — sync created product `prod_VEghKLcgCPNKkB` / price `price_1UEDJqDPxWoXhXF8WYfaEDSe` ($3.99) and `syncStripe.mjs verify` is 26/26 clean; (2) create the pack_skin Play Billing SKU (docs/store-integration.md §2 table is generated from the catalog); (3) native picker — expo-file-system documentPicker or expo-image-picker + prebuild (native build pending); (4) manual ad-hoc + e2e web iap re-run

- [ ] continuous task (do not complete): document feature map and fill out feature gaps and potential product improvements. Do not implement any feature gaps until greenlit (nothing in `docs/gap-ranking.md` is greenlit). **Pass 33 (2026-09-10):** the account / session layer — what a signed-in session owns, where erasure lives, what's verified, what's left (gap-ranking §"The account & cloud layer"): F33.1 `account:web-value` (candidate, trigger-gated on the same web-growth bets as Tier 1 #14), F33.2 `account:web-erasure` (**new Tier 1 #22** — the only in-app "delete my data" surface is gated behind the cloud provider, which is a no-op on web, so a web account has no erasure path; also fixes two stale store-integration.md release-gate references), F33.3 `account:erase-signout` (delete-my-data never signs out → dead-token session), F33.4 `cloud:stale-notice` (the last-write-wins stale push imports silently — the one of three cloud import paths with no toast). Nothing implemented — ranked only, pending greenlit. **Pass 34 (2026-09-10):** the content & data-authoring layer — what “adding one thing” (a pack, a cosmetic, a string, a save) actually touches, and the nets that catch the drift (gap-ranking §“The content & data-authoring layer”): the layer is one of the best-netted in the repo (catalog pinned five ways, the i18n tables exact, the cosmetic-name i18n, the sound-asset naming + existence, the save migration). F34.1 `docs:sku-table-sync` (Tier 2 — the §2.1 markdown ops table is the one catalog copy no test reads, still 25 rows vs the 26-row catalog; the missing row is exactly `pack_skin`), F34.2 the versioning rule (version what crosses a process boundary, normalize what doesn't — recorded, not a defect; also flags the out-of-order key-7 in the save migration map), F34.3 `release:price-placeholder-net` (Tier 2 — the `price_PENDINGPACKSKIN` placeholder passes every net in the repo). Nothing implemented — ranked only, pending greenlit. **Pass 35 (2026-09-10):** the release / distribution pipeline — build → version → gate → deploy (gap-ranking §“The release / distribution pipeline layer”): the gates are well-netted where they exist (the web e2e runs against the REAL `expo export` build, the prebuild input is byte-for-byte reproducible, store-critical config is pinned by `storeConfig.test.ts`, the disabled gates are labelled + tracked). F35.1 `release:wrangler-pin` (Tier 2 — the web deploy invokes a bare global `wrangler` that is not in `package.json` or the lockfile; `wrangler.toml` is v4-shaped, so deploy reproducibility rides on which CLI the machine happens to have), F35.2 `app:route-only-net` (Tier 2 — “ONLY route files under `src/app/`” is doc-only; the static export emits an HTML page per route, so a stray file ships as a broken public page and nothing fails), F35.3 `release:version-doc` (Tier 2, low — the bump doc directs a hand-edit of the generated `build.gradle` that prebuild rewrites, and the committed `android/` tree is already `8`/`1.0.8` vs `app.config.ts` `32`/`1.5.4`). Nothing implemented — ranked only, pending greenlit. **Pass 36 (2026-09-10):** the web discoverability / search layer — the outward contract a crawler or ad network sees (gap-ranking §“The web discoverability / search layer”). This pass LANDED the two one-off todo items (both closed): `public/ads.txt` (one `google.com … DIRECT` row derived from `storeConfig.adsense.client`) and the first SEO round (canonical + Open Graph + Twitter card + `WebApplication` JSON-LD in `+html.tsx`; `public/robots.txt` + `sitemap.xml` + `og-image.png` at the export root) — all verified in the exported `dist/`. Remaining gaps: `search:console-verify` (F36.1 — Google/Bing Search Console + AdSense account approval; human, out-of-jest, the repo half is done) and `export:seo-shape-net` (F36.2 — no test asserts ads.txt/robots.txt/sitemap.xml ship well-formed). F36.3 aliases the existing Tier 1 #14 `web:pwa` into this layer. All Tier 2, trigger-gated on the first production web release. No new Tier 1. **Pass 37 (2026-09-10):** the numeric ledger layer — where a value crosses a Number/BigInt boundary and what happens to its precision (gap-ranking §“The numeric ledger layer”): the design is strong and no player-visible defect — three bigint counters (minerals/lifetimeMinerals/maxDepth, minerals since save migration v10), one exact float bridge (`mulFloats`, scale-100 half-up) for every float×bigint, shared decimal-string bigint serialization across save/save-code/cloud, exact-integer bigint formatting in both notation modes, integer factors composed before float factors in the engine (the combo multiplier is designed integer). F37.1 `ledger:mulfloats-net` (Tier 2 — the bridge's multiple-of-0.01 invariant is asserted nowhere: no direct `mulFloats` unit test, no table test that `DEPTH_TIERS`/`PRESTIGE_LEVELS` values stay scale-100-exact — a future ×1.15 line would silently round 1/20000), F37.2 `ledger:number-boundary` (recorded, not a defect — the number-typed quartic costs cross 2^53 at level 9742 and perSec around 3.4e8×3.4e8; internally consistent since every consumer reads the same rounded number, noted so a future content pass promotes cost/perSec to bigint like v10 did), F37.3 `ledger:buildclamp-gaps` (Tier 2, low — `buildSaveData` clamps the counters and counts but the `gems` wallet passes bare `num()`: a crafted MOD1 save code imports negative/fractional gems; benign — all six gem-buy guards reject `gems < cost` — but pass 28's "clamped levels" claim overstates the net; one floor+clamp line). No new Tier 1 **Pass 38 (2026-09-10):** the dependency / supply-chain layer — the third-party code the app is built on, how it's pinned, and what a version move touches (gap-ranking §"The dependency / supply-chain layer"): the substrate is healthier than its prose suggests — Expo/React core tilde-locked to Expo's major (a fresh install can't float the framework), lockfile v9.0 committed + intact, hermetic test leg clean (the store suite imports bare `assets/`, runs in 0.35s, no live Stripe/Pocketbase/Google), one tiny crypto leaf (`pako`), no keychain/native-secret dep anywhere. F38.1 `dep:pin-drift` (Tier 2 — the native *leaf* deps are caret-pinned — expo-admob/av, react-native-purchases, react-native-mmkv, expo-updates, qrcode-svg, google-maps, cookies, splash-screen — so a fresh install floats their MINOR versions, and every local gate (typecheck/lint/jest) is JS-only, so a native ABI change from a minor bump is invisible to them; flip to tilde/exact = future-drift only, lockfile already pins exact versions), F38.2 `dep:pnpm-major` (Tier 3 — no `packageManager`/`engines`; reproducibility rests on the lockfile's `lockfileVersion 9.0` string, which matters more here because the build leans on pnpm-specific `node-linker=hoisted` layout — a one-line `packageManager` pin fixes it), F38.3 `dep:advisory-gate` (recorded — `pnpm audit` reports 4 vulns (2 high, 2 moderate), deliberately NOT chased this pass (out of scope + volatile); the real gap is there's no automated advisory gate and no recorded severity threshold while CI is disabled — a concrete decision for the release-pipeline pass), F38.4 `dep:prose-contracts` (Tier 2 — three load-bearing contracts live only in `AGENTS.md` prose with no machine check: the "keep 3 alias configs in sync" rule [found weaker than documented — jest explicitly maps only `src/`, `assets/` resolves only because jest-expo 57 honors tsconfig `paths`, and `components/`/`hooks/` are imported by no test], the "byte-for-byte prebuild" contract [the `withDebugSigning` config plugin is first-party JS, invisible to `pnpm audit`/the lockfile, and its unit test checks plugin *logic* not output-vs-pinned-SDK], and the versionCode/version "bump both" [no net]). No new Tier 1 **Pass 39 (2026-09-10):** the server-side store-verification layer — what runs when a purchase is verified, minted, and restored (gap-ranking §"The server-side store-verification layer"): F39.1 `entitlements:clobber-on-second-purchase` (**new Tier 1 #23 — real bug, not a feature gap**: the entitlement row upsert is keyed by `deviceId` alone, so a second, *different* pack bought on the same device silently rewrites the first purchase's row in place — restore and the verify response return only the last product purchased; masked in normal play by the device-local entitlement store, surfaces on reinstall or cross-device restore exactly where the server row is the recovery source; `linkDeviceRows` carries the same first-row-only assumption — the fix is small: pair-keyed upsert for `entitlements` + row-iterating backfill + a two-product regression test), F39.2 `verify:no-direct-coverage` (of the minting surface only the webhook *backup* path has a direct handler test — `handleVerify`, the PRIMARY client return-visit path, and `handleRestore` have none; the coverage gap that let F39.1 hide), F39.3 recorded, not defects (a refused verify never poisons the dedup marker — tested; webhook event-id idempotency incl. duplicate delivery — tested; trusted-source header normalization across three wire shapes — a live-probed regression; same-product re-verify idempotent, so a webhook racing the client can't double-mint; `accountId` is set-never-cleared, a property of the claim-never-copy backfill model). Nothing implemented — ranked only, pending greenlit. One stale doc reference found en route (folded into F33.2): store-integration.md still cites `pb_hooks/verify-purchase.js`, a module that no longer exists

- `docs/blockers.md` — the IAP external remnants (iOS `APPLE_*` App Store Connect
  API key for the sidecar; the Stripe `sk_live` flip at launch) and the three
  OS-level VPS hardening steps that need a human at the terminal.
- `docs/security-audit.md` — the S4/S6 manual pre-production checklist (store
  listing privacy/terms links, the 13+ questionnaire steps, the AdMob
  console "Not child-directed" setting, the post-launch S6 revisit trigger).
- `docs/backlog.md` — the intentionally deferred iOS track (AdMob iOS entry,
  App Store IAP products + credentials, iOS on-device verification passes).
- `docs/features.md` — the feature map (what exists, per section).
- `docs/gap-ranking.md` — every open gap, ranked by impact (the former
  features.md §7, moved 2026-09), with trigger-gated candidates
(including `offline:clock-hwm`, deliberately low priority per its own
sources, and `share:clipboard-opt-in`). Reopen on the signals
recorded there (first production release + the guardrail-5 signal
batch, endgame-lifetime content pass, es-market,
share-badge/cosmetics demand).
