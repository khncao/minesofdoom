# Mines of Idle Doomath — UX, Improvements & New Features Plan

Legend: [ ] not started, [o] in progress, [-] blocked
Completed items are removed from this file (see git history); only remaining work is tracked here.
Only work on continuous tasks after other tasks are completed

- [x] draft improved generated graphics (more detailed pixel art) — **LANDED 2026-09-11** — `src/utils/graphics/detailPass.ts`: a pure top-left-light bevel over the existing base grids (lit left/top edges → highlight, shadow right/bottom edges → shadow, 1px-thin parts and interiors keep base color, empty never written — identity preserved: every output color is a base color or a lighten/darken of one), two drafts `DETAIL_STRONG` (0.45/0.30) and `DETAIL_SOFT` (0.28/0.18); `detailPass.test.ts` (20 tests: shape/immutability/empties/palette-membership on all real base grids, thin-part + corner preservation, determinism, the two drafts); 4 contact sheets in `docs/art-detail/samples/` (flat baseline / detailed / detailed-soft / 8× zoom, identical sample set to the art-styles sheets); `docs/art-detail.md` with adoption notes (same cache-layer hook as the style passes; composes detail-then-style); `scripts/generate-detailed-art-samples.mjs` re-renders the sheets
- [x] ~~implement capability to swap and use any 2d art (such as sprites) for art assets (in addition to emojis and generated pixel art). Find and use some freely available art sets that are open licensed for commercial use~~ **LANDED 2026-09-11** — the custom-skin line gets a BUNDLED SPRITE LIBRARY: 8 CC0-1.0 characters (FreeGameSprites, all license-verified at the asset pages, attribution in `public/assets/sprites/CREDITS.txt`) vendored at 128×128 (dwarf 96×96 native) and compiled into a generated pure module (`bundledSprites.ts` via `scripts/generate-sprite-library.mjs`) as data URIs, so the runtime stays on the existing string-URI pipeline (skin slot `artId` → `bundledSpriteById` → `Miner playerBodyUri`); the skin slot's `artId` wins over an uploaded 16×16 grid (most-recent-intent), unknown ids normalize to null, the picker UI (thumbnails + default) lives in the IAP panel's skin row (`skin-sprite-*` testIDs) gated on the same `unlocked` flag as the uploads, names go through a new `bundledSprite` content namespace (es table + the content parity walk), gates: typecheck + lint clean, jest 1181/1181 (new `bundledSprites.test.ts` decodes every shipped data URI with the production PNG decoder; `customSkin.test.ts` pins the artId normalize/precedence nets)
- [x] improve the background blocks: ~~fix stretching (especially on wide screen)~~ **LANDED 2026-09-10** — adaptive-width strips: the PNG encoder chains stored-deflate blocks (no more 65535 raw-byte cap), `caveRowUri`/`buildCaveRow` take an optional `widthPx` (rounded up to a 24px tile multiple, capped at the block width, floored at the 14-tile default), the mined path stays centered at any width (`cavePathTiles(count)`), and `CaveBackground` measures its container and bakes each row at container width — the `Image` stretch is now ~zero on wide screens; ~~remaining: the center gap in background blocks should have blocks under the player as well, more background block variation like ores, more easter eggs (such as caves, skeletons, princesses, moles, dwarves, etc.)~~ **LANDED 2026-09-11** — (a) path tiles carry sparse rubble chunks biased to the LOWER half (`PATH_RUBBLE_CHANCE`, lower-half-only so blocks sit under the player, never over the miner) — the shaft still reads as an opening (test pins rubble < 10% of path area and y ≥ 12); (b) rock-only ore fleck veins (`ORE_CHANCE` per tier, fixed 4-color ore palette — flecks never float over gap tiles); (c) egg catalog grew 3→7 (`skeleton`, `mole`, `dwarf`, `cave` arch) with `EGG_CHANCE` 0.15→0.25, plus a test that every catalog kind rolls across a 100-combo deterministic grid; contact sheets regenerated
- [ ] finish the `packSkin` release (the in-repo half LANDED 2026-09-10 — custom skinning one-time purchase, catalog row `pack_skin`, web picker, grant path, i18n — gates green; see git history): (1) ~~run `node scripts/stripe/syncStripe.mjs products` and paste the real test price over the `price_PENDINGPACKSKIN` placeholder in storeConfig.ts~~ **DONE 2026-09-10** — sync created product `prod_VEghKLcgCPNKkB` / price `price_1UEDJqDPxWoXhXF8WYfaEDSe` ($3.99) and `syncStripe.mjs verify` is 26/26 clean; (2) ~~create the pack_skin Play Billing SKU~~ **DONE 2026-09-10** — `create-product --sku=pack_skin --title="Custom Skin" --price=3.99 --auto-convert-prices` (all regions AVAILABLE), `products-check` 27/27 clean; §2.1 table row added; (3) ~~native picker~~ **DONE 2026-09-11** — `customSkinPicker.ts` (native) now picks via expo-file-system's document picker (`File.pickFileAsync`, no new dependency, no runtime permissions; images filtered to `image/png`, audio to `audio/*`) and decodes through the same pure-JS funnels as web (the shared `pngBytesToGrid` + a new shared 16-bit PCM WAV module `utils/audio/wav.ts` — parse + encode + pure-JS base64 — which web's picker now uses for its encoder too; the 3 s / 20 MB caps are shared constants in `customSkin.ts`); `MinesOfDoom.tsx` no longer gates the upload handlers to web, the `iap.skinUploadsWebOnly` key is `iap.skinUploadsUnavailable`, toasts updated (en+es) — gates: typecheck + lint clean, jest 1170/1170 (new `wav.test.ts` + native `customSkinPicker.test.ts` with the picker mocked at the module boundary and REAL PNG/WAV fixture bytes through the production decode); no prebuild needed (expo-file-system is already a committed native dep, no manifest/permission change); (4) manual ad-hoc pass (human) + ~~e2e web iap re-run~~ **DONE 2026-09-10** — `pnpm run test:e2e:web` green 10/10 against the fresh `expo export` (boot, rewarded-ads, full IAP round-trip, sign-in; the LIVE-GSI spec is opt-in-skipped by design)

- [ ] continuous task (do not complete): document feature map and fill out feature gaps and potential product improvements. Do not implement any feature gaps until greenlit (nothing in `docs/gap-ranking.md` is greenlit). **Pass 33 (2026-09-10):** the account / session layer — what a signed-in session owns, where erasure lives, what's verified, what's left (gap-ranking §"The account & cloud layer"): F33.1 `account:web-value` (candidate, trigger-gated on the same web-growth bets as Tier 1 #14), F33.2 `account:web-erasure` (**new Tier 1 #22** — the only in-app "delete my data" surface is gated behind the cloud provider, which is a no-op on web, so a web account has no erasure path; also fixes two stale store-integration.md release-gate references), F33.3 `account:erase-signout` (delete-my-data never signs out → dead-token session), F33.4 `cloud:stale-notice` (the last-write-wins stale push imports silently — the one of three cloud import paths with no toast). Nothing implemented — ranked only, pending greenlit. **Pass 34 (2026-09-10):** the content & data-authoring layer — what “adding one thing” (a pack, a cosmetic, a string, a save) actually touches, and the nets that catch the drift (gap-ranking §“The content & data-authoring layer”): the layer is one of the best-netted in the repo (catalog pinned five ways, the i18n tables exact, the cosmetic-name i18n, the sound-asset naming + existence, the save migration). F34.1 `docs:sku-table-sync` (Tier 2 — the §2.1 markdown ops table is the one catalog copy no test reads, still 25 rows vs the 26-row catalog; the missing row is exactly `pack_skin`), F34.2 the versioning rule (version what crosses a process boundary, normalize what doesn't — recorded, not a defect; also flags the out-of-order key-7 in the save migration map), F34.3 `release:price-placeholder-net` (Tier 2 — the `price_PENDINGPACKSKIN` placeholder passes every net in the repo). Nothing implemented — ranked only, pending greenlit. **Pass 35 (2026-09-10):** the release / distribution pipeline — build → version → gate → deploy (gap-ranking §“The release / distribution pipeline layer”): the gates are well-netted where they exist (the web e2e runs against the REAL `expo export` build, the prebuild input is byte-for-byte reproducible, store-critical config is pinned by `storeConfig.test.ts`, the disabled gates are labelled + tracked). F35.1 `release:wrangler-pin` (Tier 2 — the web deploy invokes a bare global `wrangler` that is not in `package.json` or the lockfile; `wrangler.toml` is v4-shaped, so deploy reproducibility rides on which CLI the machine happens to have), F35.2 `app:route-only-net` (Tier 2 — “ONLY route files under `src/app/`” is doc-only; the static export emits an HTML page per route, so a stray file ships as a broken public page and nothing fails), F35.3 `release:version-doc` (Tier 2, low — the bump doc directs a hand-edit of the generated `build.gradle` that prebuild rewrites, and the committed `android/` tree is already `8`/`1.0.8` vs `app.config.ts` `32`/`1.5.4`). Nothing implemented — ranked only, pending greenlit. **Pass 36 (2026-09-10):** the web discoverability / search layer — the outward contract a crawler or ad network sees (gap-ranking §“The web discoverability / search layer”). This pass LANDED the two one-off todo items (both closed): `public/ads.txt` (one `google.com … DIRECT` row derived from `storeConfig.adsense.client`) and the first SEO round (canonical + Open Graph + Twitter card + `WebApplication` JSON-LD in `+html.tsx`; `public/robots.txt` + `sitemap.xml` + `og-image.png` at the export root) — all verified in the exported `dist/`. Remaining gaps: `search:console-verify` (F36.1 — Google/Bing Search Console + AdSense account approval; human, out-of-jest, the repo half is done) and `export:seo-shape-net` (F36.2 — no test asserts ads.txt/robots.txt/sitemap.xml ship well-formed). F36.3 aliases the existing Tier 1 #14 `web:pwa` into this layer. All Tier 2, trigger-gated on the first production web release. No new Tier 1. **Pass 37 (2026-09-10):** the numeric ledger layer — where a value crosses a Number/BigInt boundary and what happens to its precision (gap-ranking §“The numeric ledger layer”): the design is strong and no player-visible defect — three bigint counters (minerals/lifetimeMinerals/maxDepth, minerals since save migration v10), one exact float bridge (`mulFloats`, scale-100 half-up) for every float×bigint, shared decimal-string bigint serialization across save/save-code/cloud, exact-integer bigint formatting in both notation modes, integer factors composed before float factors in the engine (the combo multiplier is designed integer). F37.1 `ledger:mulfloats-net` (Tier 2 — the bridge's multiple-of-0.01 invariant is asserted nowhere: no direct `mulFloats` unit test, no table test that `DEPTH_TIERS`/`PRESTIGE_LEVELS` values stay scale-100-exact — a future ×1.15 line would silently round 1/20000), F37.2 `ledger:number-boundary` (recorded, not a defect — the number-typed quartic costs cross 2^53 at level 9742 and perSec around 3.4e8×3.4e8; internally consistent since every consumer reads the same rounded number, noted so a future content pass promotes cost/perSec to bigint like v10 did), F37.3 `ledger:buildclamp-gaps` (Tier 2, low — `buildSaveData` clamps the counters and counts but the `gems` wallet passes bare `num()`: a crafted MOD1 save code imports negative/fractional gems; benign — all six gem-buy guards reject `gems < cost` — but pass 28's "clamped levels" claim overstates the net; one floor+clamp line). No new Tier 1 **Pass 38 (2026-09-10):** the dependency / supply-chain layer — the third-party code the app is built on, how it's pinned, and what a version move touches (gap-ranking §"The dependency / supply-chain layer"): the substrate is healthier than its prose suggests — Expo/React core tilde-locked to Expo's major (a fresh install can't float the framework), lockfile v9.0 committed + intact, hermetic test leg clean (the store suite imports bare `assets/`, runs in 0.35s, no live Stripe/Pocketbase/Google), one tiny crypto leaf (`pako`), no keychain/native-secret dep anywhere. F38.1 `dep:pin-drift` (Tier 2 — the native *leaf* deps are caret-pinned — expo-admob/av, react-native-purchases, react-native-mmkv, expo-updates, qrcode-svg, google-maps, cookies, splash-screen — so a fresh install floats their MINOR versions, and every local gate (typecheck/lint/jest) is JS-only, so a native ABI change from a minor bump is invisible to them; flip to tilde/exact = future-drift only, lockfile already pins exact versions), F38.2 `dep:pnpm-major` (Tier 3 — no `packageManager`/`engines`; reproducibility rests on the lockfile's `lockfileVersion 9.0` string, which matters more here because the build leans on pnpm-specific `node-linker=hoisted` layout — a one-line `packageManager` pin fixes it), F38.3 `dep:advisory-gate` (recorded — `pnpm audit` reports 4 vulns (2 high, 2 moderate), deliberately NOT chased this pass (out of scope + volatile); the real gap is there's no automated advisory gate and no recorded severity threshold while CI is disabled — a concrete decision for the release-pipeline pass), F38.4 `dep:prose-contracts` (Tier 2 — three load-bearing contracts live only in `AGENTS.md` prose with no machine check: the "keep 3 alias configs in sync" rule [found weaker than documented — jest explicitly maps only `src/`, `assets/` resolves only because jest-expo 57 honors tsconfig `paths`, and `components/`/`hooks/` are imported by no test], the "byte-for-byte prebuild" contract [the `withDebugSigning` config plugin is first-party JS, invisible to `pnpm audit`/the lockfile, and its unit test checks plugin *logic* not output-vs-pinned-SDK], and the versionCode/version "bump both" [no net]). No new Tier 1 **Pass 39 (2026-09-10):** the server-side store-verification layer — what runs when a purchase is verified, minted, and restored (gap-ranking §"The server-side store-verification layer"): F39.1 `entitlements:clobber-on-second-purchase` (**new Tier 1 #23 — real bug, not a feature gap**: the entitlement row upsert is keyed by `deviceId` alone, so a second, *different* pack bought on the same device silently rewrites the first purchase's row in place — restore and the verify response return only the last product purchased; masked in normal play by the device-local entitlement store, surfaces on reinstall or cross-device restore exactly where the server row is the recovery source; `linkDeviceRows` carries the same first-row-only assumption — the fix is small: pair-keyed upsert for `entitlements` + row-iterating backfill + a two-product regression test), F39.2 `verify:no-direct-coverage` (of the minting surface only the webhook *backup* path has a direct handler test — `handleVerify`, the PRIMARY client return-visit path, and `handleRestore` have none; the coverage gap that let F39.1 hide), F39.3 recorded, not defects (a refused verify never poisons the dedup marker — tested; webhook event-id idempotency incl. duplicate delivery — tested; trusted-source header normalization across three wire shapes — a live-probed regression; same-product re-verify idempotent, so a webhook racing the client can't double-mint; `accountId` is set-never-cleared, a property of the claim-never-copy backfill model). Nothing implemented — ranked only, pending greenlit. **Pass 40 (2026-09-10):** the tick / time-scheduling layer — every clock in the repo and what freezes each (gap-ranking §“The tick / time-scheduling layer”): the load-bearing invariant holds — exactly two timer shapes exist (the catch-up engine loop and deadline pollers) and neither counts fires, so iOS suspension, Android kill, and Chrome tab-throttling can delay but never lose state; bfcache/sleep/process-death survive with zero `resume` code by construction. F40.1 `tick:registry-not-a-clock` (Tier 2, low — the documented 1Hz `onTick` clock is actually “1Hz while passive income is non-zero”: the registry fires only inside the miners-gate + after the `elapsed<1` early return, yet `Context.tsx` doesn't state the gate — that gate is exactly why the eight deadline pollers correctly reinvent 1Hz each with wall-clock deadlines; one-line doc fix, optional registry promotion), F40.2 `tick:catchup-untested` (Tier 3 — the `min(max(0, floor((now-last)/msPerTick)), maxOfflineTicks)` expression the absence economy depends on is inline in `useGameEngine` and untested, while its sibling `activePlaySeconds` is extracted + fully tested; extract to a pure `game.ts` helper). Recorded: the pre-load clobber race is netted by the `loadedRef` guard in `saveGame`; autosave is tick-counted with a 5s floor so throttling can't stretch or disable it; the ≤50ms tap-flush window can drop a frame of unflushed taps on immediate tab close (bounded ~a tap or two); clock phases are independent and no consumer diffs two clocks; the live-hidden-tab-vs-restarted absence asymmetry is pass 17 finding (2)'s accounting, ranked there. Nothing implemented — ranked only, pending greenlit. One stale doc reference found en route (folded into F33.2): store-integration.md still cites `pb_hooks/verify-purchase.js`, a module that no longer exists. **Pass 41 (2026-09-10):** the external-code & ambiguous-result layer — the code the shipped build executes from outside the bundle (the two web runtime script loaders, the static AdSense tag) and the local state that reacts to ambiguous VPS results (gap-ranking §"The external-code & ambiguous-result layer"): the GSI loader is the repo's correct pattern (fresh element per attempt, load timeout, typed cancel) and the fix template; every other ambiguous-result site is non-destructive (cloud pull → "no backup", IAP verify → queue, leaderboard → unavailable) except one. **Two new Tier 1s, both real bugs:** #24 `web:stripe-script-retry-hang` (F41.1 — `loadStripe`'s error path resets the promise cache but leaves the dead script element; the next purchase re-attaches listeners to an already-settled script that never fires again → the promise never resolves → the `useIap` in-flight guard's `.finally` never runs → the shop is dead for the page session; fix: mirror the GSI loader one file away) and #25 `account:me-network-wipes-token` (F41.2 — `storeAuthProvider.me()` folds 401 / any non-2xx / transport failure into one `null`, and `useAccount`'s mount restore reads `null` as "dead session" and clears the stored 30-day token → a cold start offline or through one VPS blip silently signs the player out; fix: tri-state `me()`, wipe only on an explicit 401). Companions recorded: F41.3 GSI dead-element accumulation (Tier 3), F41.4 `loadStripe` caches a null resolve forever (Tier 3, fold into F41.1), F41.5 200-with-malformed-body also wipes the token (sibling of F41.2 — already covered by its fix shape; needs a test-matrix case). Nothing implemented — ranked only, pending greenlit. **Pass 42 (2026-09-11):** the test & verification layer — the nets themselves (gap-ranking §“The test & verification layer”): 77 jest suites / 1,170 tests in ~7s, hermetic; Playwright web e2e (boot/ads/iap/signin + ad-test-mode export server + network stubs); manual probe scripts. F42.1 `test:worker-force-exit` (Tier 2 — every full-suite run ends in jest’s “worker failed to exit gracefully, force exited” with a scheduler.setImmediate stack: 20 `render()` calls across 5 hook suites (useLeaderboard ×12, useCloudSave ×4, haptics, useMineTaps, useEquations) have zero `unmount()`/`cleanup()` (nativeStackWiring is the correct template); masks future teardown regressions and makes full-suite `--detectOpenHandles` unreliable; the earlier “suite hangs past 300s” is the same leak’s extreme tail; fix is 20 mechanical unmounts), F42.2 `tree:concurrent-mutation` (Tier 2, methodology — the working tree mutated mid-audit: pb_hooks changed layout vintages and 6 WIP files sat uncommitted (the in-flight native skin-picker release); file-level coverage findings (F39.2, the 401 gap) must be re-verified against the current tree before any fix pass is greenlit), F42.3 `pb:checkout-401-untested` (Tier 3, re-verified on the current 10-test vintage — zero `401` occurrences in pb_hooks/**test** — the unauthenticated checkout-session path is still untested). Nothing implemented — ranked only, pending greenlit. **Pass 43 (2026-09-11):** the asset-generation & shipped-artifact layer — how the shipped art and audio come to exist (gap-ranking §“The asset-generation & shipped-artifact layer”): five `scripts/generate-*.mjs` compilers (sprite library → `bundledSprites.ts`; pickaxe sounds + ambient loop → `public/assets/audio/`; two doc sample-sheet renderers) plus the in-code grid builders (`pixelArt.ts` / `caveTiles.ts`). The nets are uneven: the sprite library's *shipped output* is well-netted (`bundledSprites.test.ts` decodes every data URI with the production decoder), the grid builders and the art passes are netted (`pixelArt`/`caveTiles`/`stylePasses`/`detailPass` tests), but the generator→artifact *reproducibility* contract is doc-only, and the two sample-sheet scripts carry no net at all (the contact sheets rot silently when a builder changes). F43.1 `gen:regen-net` (Tier 2 — no test re-runs any generator and byte-compares the committed artifact; the scripts are deterministic by design, so a temp-dir regen + byte-diff is a cheap, high-signal net; same class as F35.2: a doc-only contract with no machine check), F43.2 `gen:script-dup` (Tier 3 — the two sample-sheet scripts each inline their own zlib PNG writer + the extensionless-import `registerHooks` block; extract to a shared helper before a third renderer appears). Recorded, not defects: the vendored CC0 source PNGs ship in the web export (public/ is the export root) on top of the compiled data URIs — a small size cost that buys prod-discoverable `CREDITS.txt` attribution. Nothing implemented — ranked only, pending greenlit. **Pass 44 (2026-09-11):** the React state & component-architecture layer — how the 1Hz tick drives re-renders and what isolates what (gap-ranking §"The React state & component-architecture layer (pass 44 — …)"): the layer is already far more isolated than its shape suggests — all 30 game components are `memo`-ized, the root deliberately builds every memo-sensitive value once outside JSX, and the tick path is engine → context → memoized children. F44.1 `docs:appcontext-orphan` (fixed this pass — `src/AppContext.ts` had been orphaned by the iteration-28 move to `mines_of_doom/Context.tsx` with zero importers; deleted + AGENTS.md synced) and a follow-up folded into the same `src/` extraction todo already recorded there (the tick-driven canvas subtree is the only root-fragile spot: a future un-memoized panel added as a sibling re-mounts on every 1Hz tick). Nothing implemented — ranked only, pending greenlit.
**Pass 45 (2026-09-12):** the compliance / privacy / age-rating layer — what a regulator, a store reviewer, and a parent see when they look at the shipped product (gap-ranking §"The compliance / privacy / age-rating layer (pass 45 — …)"): the layer is built well — policy v2.0 written against the real architecture with the generated `public/*.html` test-pinned (`legalDocs.test.ts`), the S6 decision (teen+ 13+, NOT child-directed) recorded and `tagForChildDirectedTreatment: false` test-pinned, and every policy data-flow claim re-verified against its module (save codes carry no tokens, analytics local-only, crash ring save-free, token in Keychain/Keystore, salted KDF, leaderboard rows = chosen name + scores, share images PII-free, card data never client-side). F45.1 `compliance:policy-child-directed-mismatch` (**new Tier 1 #26** — the published policy's "Children" section asserts ads are "configured for child-directed treatment", contradicting the S6 decision made two days after the policy's effective date; one-sentence rewrite + HTML regeneration), F45.2 `web:adsense-consent-gate` (Tier 2 — the production web export emits the live AdSense loader into every page's `<head>` with no consent mechanism, and the policy is silent on third-party cookies; trigger-gated on the AdSense account approval). Nothing implemented — ranked only, pending greenlit.

**Pass 46 (2026-09-12):** the storage / data-footprint layer — what the app stores where, how big each store grows over time, and what a platform quota or erasure can reach (gap-ranking §"The storage / data-footprint layer (pass 46 — …)"): the footprint is small and bounded by design — the save is one O(1) JSON blob in AsyncStorage/MMKV, the ad-credential store is fixed-size by construction (one row per `storeConfig.ts` constant), the entitlement log is capped at 200, and the cloud side is one row per account, and every bound that exists is test-pinned. The one store with **no** bound is the crash ring, and its sole size net is only an indirect one. F46.1 `storage:crash-ring-unguarded` (Tier 2 — the ring's 32KB cap has no direct eviction test: the 256-entry count cap is pinned, the per-entry JSON cap is pinned, and an *indirect* test proves the cap binds, but nothing pins the cap constant itself to the eviction path — a future constant bump could ship an unbounded-in-practice store in the one location that runs from the error boundary, and web crash logs sit in AsyncStorage outside the policy's "data you delete" scope; a two-line `CRASH_LOG_MAX_BYTES` eviction test), F46.2 `docs:policy-local-storage-list` (Tier 3 — the privacy policy's local-storage list omits the custom-skin picker's ~10 MiB grid-URI cache (a bounded but non-trivial, explicitly local-only, erased-on-reset store); a policy-content fix for the same regeneration net as F45.1/F45.2). Recorded, not defects: the cloud save blob shares the save's bounded shape + the 16KB server cap is test-pinned; erasure is total across every local store (the MMKV/AsyncStorage dual-write is drained by the erase test); `crashLog:erase-on-reset` is the same reset-vs-erase asymmetry already recorded in pass 44; the grid-URI cache's bytes never reach the server (base64 in the save is 64× smaller; the 16KB cap would reject it anyway). Nothing implemented — ranked only, pending greenlit.

**Pass 47 (2026-09-12):** the listener & subscription lifecycle layer — every registration with the platform: who registers, who removes, and what dies with the screen (gap-ranking §"The listener & subscription lifecycle layer (pass 47 — …)"): every long-lived registration is paired with a removal — the three AppState subscriptions (save-on-background, active-ref, bed pause) all `.remove()`; the DOM listeners (first-gesture gate, visibilitychange, pagehide, matchMedia, the one-shot script pairs) are removed or genuinely one-shot; the whole timer family (engine loop + in-loop autosave, the five 60s/5s key pollers, the two 1s countdown clocks, the juice-wave Set, the combo-save expiry, the double-RAF URL re-clean) is cleared; the expo-audio players are paused + nulled on cleanup; the AdMob flow is first-terminal-wins with `removeAllListeners()` + a watchdog cleared on OPENED; the AdSense watchdog clears in `settle`; the HTTP timeout pattern is request-bounded by construction. The four unpaired sites are bounded fire-and-forget timers in the never-unmounted root-screen tree (so the safety argument is architectural, not per-site), and the hardware-back path is deliberately unintercepted — correct for a single root `Stack.Screen`, recorded as a watch-item for the day a second route exists. F47.1 `lifecycle:fire-and-forget-timers` (Tier 3 — `useMessages`' timeout, `useMineTaps`' self-rescheduling rAF chain, and the `BlockBreak`/`DebrisParticles` per-batch removals have no cancellation path; none a live defect while the root screen never unmounts; fix shape = a ref + cleanup per site, zero behavior change, only worth doing if a future pass makes any of these unmountable). Nothing implemented — ranked only, pending greenlit.

**Pass 48 (2026-09-12):** the fault-handling / failure-path layer — every site where data is malformed or the platform throws, and who hears about it (gap-ranking §"The fault-handling / failure-path layer (pass 48 — …)"): the layer is end to end one of the best-fault-handled in the repo — all ten production `JSON.parse` sites are guarded with a degrade path (the save loader is the model: three wrapped stages + corrupt-save *quarantine* to `saveDataKey + ".corrupt"`), the crash net is double-layered on native (render boundary + `ErrorUtils` global wrapper, `recordCrash` contained twice — serializer and its own persistence), every platform-"no" funnel degrades to a typed toast-able result (HTTP 401/409-mapped, secure-token keychain-throw → in-memory, audio play-rejections caught, the share fallback ladder, the skin pickers' decode results), and the equation generator has a can't-fail playability fallback. Both gaps are *symmetry* gaps: F48.1 `fault:web-global-capture-missing` (Tier 2 — `installGlobalErrorCapture` bails on web (react-native-web has no `ErrorUtils`), so the web release build has **no global capture**: out-of-React throws on web reach only the console and the crash ring / AboutTab "Recent errors" stay empty — even though `recordCrash` itself is fully web-compatible; the native install wrapper also has no direct unit test; fix = a web branch on `window` error/unhandledrejection → `recordCrash(…, "global")` + nets for both branches), F48.2 `fault:skin-picker-raw-reject` (Tier 3 — the pickers type *decode-level* failures, but the file-read layer underneath can still throw raw (native `File.pickFileAsync`/`arrayBuffer()`, web `file.arrayBuffer()`), and `MinesOfDoom`'s two upload handlers await with no try/catch → an FS/IO failure is a silent dead upload button (no toast), breaking the module's own plain-language-failure contract one layer below the typed boundary; fix = wrap the picker bodies into an `io` invalid variant or catch at the call sites). Recorded, not defects: the deliberate no-toast on picker cancel; the dead try/catch around `parseFloat` in `useEquations`; the DOM-detached `<input>` in the web picker's `chooseFile` (browser-compat watch item — a non-firing click reads as a dead button with no failure signal); the boundary sitting inside the stack (a native-stack synchronous throw would white-screen — low probability, the suspected Android class is the global net's); the 32 KB crash-ring cap net is F46.1, not re-ranked; `recordCrash`'s containment shape is the layer's model. Nothing implemented — ranked only, pending greenlit.

**Pass 49 (2026-09-13):** the concurrency / async-race layer — two writes to the same byte, and who wins (gap-ranking §"The concurrency / async-race layer (pass 49 — …)"): the layer is mostly disciplined — the crash ring's read-modify-write is serialized on a module-scope promise chain (the layer's model pattern), the ref-folded setters (analytics, daily bonus) fold synchronously inside callbacks so same-turn records see each other, `useLocalStorage`'s `dirtyRef` net holds, the engine's five save triggers read synchronously-serialized snapshots so in-flight `setItem`s are FIFO-behind newer snapshots (no stale overwrite on the bridge queue or web's synchronous localStorage), and the cloud push is single-flight by ref with a wall-clock cadence. F49.1 `identity:device-id-fork` (**new Tier 1 #27 — real bug**: `getIapDeviceId()` is an unchained read-generate-write with no memo, called per round-trip by all six server-facing modules — on a fresh install the first autosave fires cloud-push AND leaderboard-submit in the same commit, both read the empty storage key before either writes → two distinct `dev-…` ids minted, last `setItem` wins → the first cloud backup row is keyed by the losing id, orphaned forever and unreachable by "delete my data"; invisible to every local gate because the dev-sim providers never call it; fix ≈ 8 lines in `iapDeviceId.ts` copying the crashRing chain pattern + a two-concurrent-calls-must-resolve-the-same-id net), F49.2 `account:restore-vs-signin` (Tier 3 — `useAccount`'s mount-restore continuation is unconditional: a register/login completed inside its `me()` window has its new token wiped + `setStatus("out")` stomping the fresh `"in"`, or on a 30-day token the just-signed-in account replaced by the stored one; fix = apply the continuation only when `sessionRef.current === null` or the tokens match), F49.3 `web:multitab-fork` (Tier 3 — no `storage` event listener anywhere in `src/`: two same-origin tabs run independent engines over every shared key, last write wins; the sharpest loss is a cross-tab same-day daily-bonus double-claim, the sibling of the bug the in-tab ref fix deliberately kills; fix = re-import on claimable-state keys or visible-tab-only autosave). Recorded, not defects: StrictMode double-invocation idempotency, the `replayPendingVerifies` un-chained RMW (idempotent re-verify, safe direction), the `pending` flag blur (UI-only), the sub-user-action analytics overwrite window, `session.ts` pure derivation. Nothing implemented — ranked only, pending greenlit.

**Pass 50 (2026-09-14):** the server API surface layer — the PocketBase hooks + sidecar routes: who reaches what, and what it can spend (gap-ranking §"The server API surface layer (pass 50 — …)"): first end-to-end audit of the route surface itself (pass 39 audited the verification *logic*, pass 37 the webhooks) — `app.pb.js`'s three PB routes, `sidecar/server.js`'s two local routes, `endpoints.js`'s platform split, and the shared libs' wiring. Verdict: the surface is clean — per-route auth is correct on every route (checkout admin 401/403 split, webhook signature-only, sidecar secret-only with generic 400 on non-JSON and a redacted 500 `detail`), payload caps are right (sidecar 50KB → 413; PB routes on PocketBase defaults), error shapes are uniform `{error: <stable code>}` with no stack leakage on any prod path, the sidecar is bound 127.0.0.1:8787 with CORS locked to localhost origins, and the no-rate-limit posture is documented (storeVerify's 10/day cap + checkout's 60s dedup + webhook idempotency are the mitigations, consistent with pass 49 #23's apparmor deferral). One defect: F50.1 `compliance:pb-portal-backdoor` (**new Tier 1 #28 — needs owner call**): the PB-side `POST /api/stripe-portal` is a documented dev backdoor with **zero production traffic** (the live flow is app → sidecar → `logic.createCustomerPortalLink` directly — the sidecar never calls the PB HTTP handler) yet still unauthed and unratelimited on the public port — anyone reaching PocketBase can send Stripe portal-link emails to arbitrary customer addresses at any rate (email impersonation from the store's domain; no purchase, no DB write, no data access), and it is the only PB route with **no test at all** — the sibling routes have handler-level suites but no route-table test asserts per-route auth posture. Fix: owner call — (a) remove the PB-side handler + update the §2.6/§2.7 doc note (the sidecar is the supported path, recommended) or (b) add the standard `authModel.role === "admin"` guard; either way add the route-table test (each registered handler invoked with authed/unauthenticated fake contexts, assert 401/403 vs pass-through) to lock the whole surface. Recorded, not defects: checkout's 60s dedup is per-process in-memory (documented single-process deploy — a multi-replica PB would bypass it; fix would need a PB-backed store, out of scope at one instance). Nothing implemented — ranked only, pending the owner's remove-vs-gate call.

**Pass 51 (2026-09-15):** the juice / game-feel feedback layer — what a mine feels like: the wave model, the haptic/sound/particle/shake channels, and the reduce-motion gate (gap-ranking §"The juice / game-feel feedback layer (pass 51 — …)"): one wave model (`juice.ts` — waves + floating-text size, one per decimal digit of the gain, capped at 5) drives the swing waves, debris repeats, floating-text size, and tap-haptic duration, so the channels can't drift apart; the feedback is bounded at every site (waves 5, particles 12, blocks 5, floating texts 16, 50 ms haptic throttle, 20 Hz tap flush), and the reduce-motion principle ("suppress the decorative repeats, keep the action's own hit" — manual toggle OR'd with web `prefers-reduced-motion`) is applied to 7 of 9 motion channels. The two exceptions are the two highest-frequency motion sites: F51.1 `accessibility:juice-motion-bypass` (Tier 2 — the answer-field shake on every wrong answer (±10 px / 400 ms, called unconditionally from both `onIncorrect` and `onSoftIncorrect`) and the floating "+N" float-up on every gain (`FloatingTextLayer` takes `_props` and ignores them) bypass the gate the combo flash, miner bob, pocket pulse, debris, and pill/loading pulses all respect; fix = two small gates — no-op `shake()` under the flag + a `reduceMotion` prop on the floating layer that swaps the float-up for a fade-in-place — plus the existing useMineTaps/useCombo test pattern extended to the two sites), F51.2 `juice:wave-interval-net` (Tier 3 — `JUICE_WAVE_INTERVAL_MS` = 130 must stay above the three targets' local throttle constants (Miner 100, Debris/BlockBreak 80) or wave 2+ is silently dropped; the constants aren't exported and no test pins the invariant — export + one-line net), F51.3 `haptics:dead-export` (Tier 3 — `getTapHapticDuration` is exported + 5-test-asserted but production uses the parallel `getHapticPattern("tap", …)` branch; delete or route the live path through the tested function). Recorded, not defects: the `BlockBreak` pop is deliberately un-gated (the "action's own hit" the collapse rule keeps); haptics being un-gated by reduce-effects is correct (different modality, own toggle; web `Vibration` is a platform no-op); the sound side of juice scaling is already Tier 1 #6; the `dailyEquation` double-haptic drops the second beat under the 50 ms throttle (the success beat wins, which reads correctly); OnboardingOverlay is static by design; the shared animation clock is unit-tested; wave-timer stacking under tap spam is bounded by tap rate (F47.1's fire-and-forget family). Nothing implemented — ranked only, pending greenlit.

**Pass 52 (2026-09-15):** the onboarding / first-session flow — the one-time tutorial and the save-code round trip (gap-ranking §"The onboarding and first-session flow (pass 52 — …)"): first end-to-end walk of the player-entry journey — the 4-step `OnboardingOverlay` (three loop tips + the first-time setup step, `TOTAL_STEPS = TIP_KEYS.length + 1`), the `onboardingDone` flag (a `useLocalStorage` key deliberately *separate* from the save), the single dismiss path (one `onDismiss` closure reached from both Skip and "Start mining!"), and the save-code export/import pair with its settings ride-along. Verdict: the flow is honest and the copy is accurate — every tip claim checks out against the engine (combo is broken by wrong answers and cave holds, not a timer; tip 3 points at the 🎯 goals column; the setup step deliberately reuses the `settings.opName.*`/`settings.onScreenKeypad` keys so the rows are recognizable in the menu). The gaps are at the two seams where "first session" and "resumed session" diverge: F52.1 `onboarding:save-code-import-replays-tutorial` (Tier 2 — `onboardingDone` is not in the `SaveCodePayload` (`SaveData & {settings?, equationSettings?}`) and the import path never touches it, so a returning player who restores a veteran save on a new device via save code — exactly the feature's point — gets the 4-step tutorial replayed; fix = an additive optional `onboardingDone?: boolean` on `SaveCodeSettings` following the existing ride-along round-trip pattern, written on export and applied in `handleImportSaveCode` when `true`; prestige is deliberately not a seam — `sinkNewShaft` never touches the flag), F52.2 `onboarding:no-replay-path` (Tier 3 — the flag has exactly one write site (the dismiss closure) and no clear site anywhere (grep-verified: no replay row in any settings component), so a skipped player who then got lost can't re-read the tips; the overlay is a static tip sheet, so replaying mid-run is safe; fix = a settings row that sets the flag `false` + i18n in en/es). Recorded, not defects: the loading gate is deliberate anti-flash (overlay hidden + screen unfocusable while the flag loads, L1620/L1809); the idle-reminder gate is correct (`useIdleReminder` disabled while onboarding, L824–832); tip 2's copy makes no timer claim (the idle reminder toast is a separate feature); the engine's `void settings` in `importSaveCode` is the settings-agnostic layering contract (the caller applies the ride-along, same pattern as `restoreFromBlob`'s `onSettings`); the setup step's i18n key reuse and the a11y labels (`onboarding.a11ySkip/Next/Start`) are by design. Nothing implemented — ranked only, pending greenlit.

**Pass 53 (2026-09-15):** the input / answer-entry layer — every path player intent takes to the game (gap-ranking §"The input / answer-entry layer (pass 53 — …)"): first inventory of the entry paths themselves — the two answer paths (the OS-keyboard `TextInput` in `AnswerInput`, the `NumericKeypad` strip in `src/components/` — both funnelling into `useEquations.handleSubmit`), the cave's hold-to-mine responder in `MiningCanvas`, the `IntegerInput` numeric-setting rows, and the parse boundaries (`parseFloat` in `handleSubmit`, `parseInt` in `IntegerInput`, the generator's own clamps). Verdict: the layer is deliberately over-built for stability (stable callbacks so the memoized input skips the 1Hz tick, a responder-system canvas instead of Pressable, the OS keyboard fully avoidable via the keypad) — and the one real gap is that an *empty* answer is parsed as a *wrong* one: F53.1 `input:empty-submit-penalized` (Tier 2 — `handleSubmit` has no empty guard: `parseFloat("")` → NaN, `approxeq(NaN, answer)` → false → the **full** `onIncorrect` path (stone sound + shake + error haptic + resistant combo reset + toast) for typing nothing; both entry points reach it with zero input — Return in an empty box via `onSubmitEditing` (AnswerInput L110) and the keypad `=` key (`onSubmit={handleSubmitActivity}`, L1728); fix = one early return `if (textInput.trim() === "") return;` before the parse, covering the soft-mode branch too, plus the missing `useEquations.test.ts` net: an empty submit fires neither `onIncorrect` nor `onSoftIncorrect` and rolls no new equation), F53.2 `i18n:cave-hold-label-hardcoded` (Tier 3 — the cave's visual `hold to mine` label (MiningCanvas L324) is hardcoded English while its accessibility twin is i18n'd (`a11y.holdToMine` in en+es) — a Spanish player hears "Mantén pulsado para minar" and reads English; fix = add a content key to both files, one line each). Recorded, not defects: the generator clamps hostile settings at the boundary (`getRandomEquation` floors `minNumber` at 0 / `maxNumber` at 2, L291, multiply fallback), so `IntegerInput`'s unclamped `parseInt` and the save-code ride-along's unclamped `equationSettings` are harmless at the parse boundary (stored value odd, generated equation not); `IntegerInput`'s loose parsing is bounded/cosmetic (empty → `clear()` + keep last valid; `"3.5"` applies 3 while showing 3.5; uncontrolled `defaultValue` doesn't refresh on import ride-along); the keypad's digit cap is the shared `MAX_ANSWER_LENGTH` (12, applied in MinesOfDoom's `handleKeypadDigit`, L1007), the bottom row is four deliberate 0s, ⌫ long-press = clear-all (a11y hint); the cave's quick-tap no-op (MINE_HOLD_MS = 300) is documented design; `onSubmitEditing`'s imperative `clear()` is belt-and-braces (all `handleSubmit` branches already `setTextInput("")`; RN `clear()` is a web no-op — the controlled prop covers web); no separate physical-key handler on web (RNW's TextInput is a DOM input; `inputMode` is a hint, not a filter); the `latestRef` stability pattern is the layer's design win (the 20 Hz tap flush / responder canvas are pass 20/47/51 territory). Nothing implemented — ranked only, pending greenlit.

- `docs/blockers.md` — the IAP external remnants (iOS `APPLE_*` App Store Connect
  API key for the sidecar; the Stripe `sk_live` flip at launch) and the three
  OS-level VPS hardening steps that need a human at the terminal.
- `docs/security-audit.md` — the S4/S6 manual pre-production checklist (store
  listing privacy/terms links, the 13+ questionnaire steps, the AdMob
  console "Not child-directed" setting, the post-launch S6 revisit trigger).
- `docs/backlog.md` — the intentionally deferred iOS track (AdMob iOS entry,
  App Store IAP products + credentials, iOS on-device verification passes).
- `docs/features.md` — the feature map (what exists, per section).
- `docs/gap-ranking.md` — every open gap, ranked by impact (the former
  features.md §7, moved 2026-09), with trigger-gated candidates
(including `offline:clock-hwm`, deliberately low priority per its own
sources, and `share:clipboard-opt-in`). Reopen on the signals
recorded there (first production release + the guardrail-5 signal
batch, endgame-lifetime content pass, es-market,
share-badge/cosmetics demand).
