# Mines of Idle Doomath — UX, Improvements & New Features Plan

Legend: [ ] not started, [o] in progress, [-] blocked
Completed items are removed from this file (see git history); only remaining work is tracked here.
Only work on continuous tasks after other tasks are completed

- [x] pb_hooks auth-plane direct coverage (pass 71) — closes F39.2 (`verify:no-direct-coverage`) in full: the remaining auth handlers had no direct tests (only their pure halves — KDF, sessions, provider-merge — were netted by `logic.test.js`). New `pb_hooks/__test__/handlerAuth.test.js` (18 tests) drives the real handlers on the shared `fakeDatastore.js` fake in identityVerify's FAKE-TOKEN sandbox mode (`MDOOM_DEV_FAKE_TOKEN=1` via `loadHandlers`' env override, so the idToken payload is trusted and no network is touched): register (account + 64-hex session + KDF hash + the sign-in backfill tagging every device cloudSave/entitlement row), login (wrong-password and unknown-email return the SAME 401 — the endpoint must not confirm which half of the credential is wrong; a legacy single-iteration `sha256:<salt>:<hash>` row is transparently upgraded to the pbkdf2 KDF on successful login and still verifies on the next one), provider sign-in (first sign-in pins `googleId` + email; a second device with the same `sub` signs into the SAME account; a verified email merges into a pre-existing email account instead of forking it; unparseable tokens and bad-sub tokens are hard 401s that create no accounts and no sessions), me/logout (live session → account shape; garbage/expired tokens 401 and expired rows are pruned in place; logout is idempotent), set-password (the session IS the ownership proof — no old-password input; an email-less account is refused because login is keyed on email; after a change the old password 401s and the new one logs in), `auth/link` (a new device's anonymous cloudSave/leaderboard rows are attached to the account — backfill only, nothing copied), and `auth/link/google` (a provider id owned by another account is a 409 `provider-taken` — never steal, never merge; success pins the id and an unclaimed verified email). **Every handler in the `handlers` map now has direct coverage** (verify/restore pass 69, data plane + delete pass 70, webhook `handlerStripeWebhook.test.js`, auth this pass). All gates green (typecheck, 89 suites / 1331 tests, lint).
- [x] pb_hooks data-plane direct coverage (pass 70) — the F39.2 (`verify:no-direct-coverage`) follow-up: the cloud push/pull, leaderboard-trio, and GDPR-delete handlers had no direct handler tests — only the pure logic (`logic.test.js`) netted them, and F39.1 (pass 39) proved how far that leaves the wiring layer. Extracted pass 69's production-faithful fake into the shared `pb_hooks/__test__/fakeDatastore.js` (records bound to their stored rows; `set()`/`save()` mutate in place) and refactored `handlerVerify.test.js` on top of it (behavior unchanged, 6/6 green). New `handlerDataPlane.test.js` (18 tests) drives the real handlers: `cloud/push` last-write-wins (an OLDER push never clobbers a newer stored blob; the 30/hour write budget 429s and writes nothing), `cloud/pull` account union (newest `updatedAt` across every device linked to the account wins; the caller's own row wins a timestamp tie — a sibling device can't shadow local data), the leaderboard trio (submit is a merge — bests only go up, achievement-id sets union; top-N is best-first with a limit; rank = strictly-above + 1), and `delete` (device scope wipes cloudSaves/leaderboard/events but PRESERVES entitlements — the refund/restore guarantee; account scope additionally erases the account's entitlements, sessions, and account row; other devices' data untouched). The remaining auth handlers were closed in pass 71. All gates green (typecheck, 88 suites / 1313 tests, lint).
- [x] entitlements clobber fix — Tier 1 #23 (pass 69) — FIXED `entitlements:clobber-on-second-purchase` (found pass 39, F39.1): the shared writer upserted entitlement rows keyed on `deviceId` ALONE, so a second, *different* pack bought on the same device rewrote the first pack's row in place — restore and the verify response returned only the last product purchased, and the first pack's entitlement was gone from the collection. Masked in normal play by the device-local entitlement store; it surfaced on reinstall or cross-device restore — exactly where the server row is the only recovery source. Fix as F39.1 specified: a new pair-keyed `upsertEntitlementRow` (findRecordsByFilter on `deviceId && productId`) used at BOTH mint sites (primary verify + webhook backup); `cloudSaves`/`leaderboard` keep the single-row-per-device shape where it's correct; and `linkDeviceRows` (the sign-in backfill) now tags EVERY entitlement row, not just the first (its blast radius). New `pb_hooks/__test__/handlerVerify.test.js` (6 tests) drives the real handlers against a production-faithful fake — records bound to stored rows, `set()` + `save()` mutating in place (the property the old webhook fake modeled as the *intended* upsert, which is why the bug hid): two products/one device → two rows on both mint paths, restore returns both, same-product re-verify stays one row (refreshed in place), webhook dedup intact, and the backfill regression. Regression proof: the three bug tests FAIL against the pre-fix handler (verified by stashing the fix) and pass with it. All gates green (typecheck, 87 suites / 1295 tests, lint).
- [x] hook-coverage audit: daily bonus + messages (pass 68) — continued the pass-67 hook-coverage sweep over the remaining untested hooks. `useDailyBonus` (the hook around the already-covered pure `dailyBonus.ts` math) had no test of its state machine, so the unlimited-claim fix it carries — the state ref updated synchronously so a fast second tap before the re-render cannot pay the streak bonus twice — was untested at hook level; `useMessages` (the single-slot toast state behind all 21 display-message call sites) had no tests at all. Added `useDailyBonus.test.ts` (4 tests, fake timers + in-memory AsyncStorage mock, same pattern as `useSettings.test.ts`): fresh start claims the base bonus exactly once (double-tap blocked, state persisted with today's day key), already-claimed-today is a no-op, next local day continues the streak with the grown ladder bonus, and day 7 pays the 250k milestone not the ladder. Added `useMessages.test.ts` (4 tests, fake timers, no mocks): starts empty, a message lives until its timeout then clears, a new message replaces the old one and resets the expiry clock (the old timer is cancelled — a late expiry can't wipe the newer message), and re-displaying the same message refreshes its clock. Residual untested hooks are thin adapters over already-tested pure modules (`useDailyEquation`, `useIdleReminder`, `useShakeInput`, `useSounds`, `useJuiceWaves`, `useWeeklyChallenge`, `useCustomSkin`, `useAnalytics`, `useCrashLog`) — noted, not pursued. All gates green (typecheck, 86 suites / 1289 tests, lint).
- [x] mineral-pocket layer audit (pass 67) — the pure module (`gemPocket.ts`) was already covered by `gemPocket.test.ts`, but the hook (`useGemPocket`) had no tests: its 1 s check loop, spawn toast, collect-once ref guard, real 30 s fade, and 5-min cooldown gating were untested. Added `useGemPocket.test.ts` (7 tests, fake timers + mocked `Math.random` + stubbed engine callbacks): spawn toast fires once, collect grants the frozen bonus exactly once (fast double-tap), an untouched pocket fades paying nothing, no respawn inside the cooldown, respawn once past it, and `enabled:false` (onboarding) never spawns. All gates green (typecheck, 84 suites / 1281 tests, lint).
- [x] remove the calendar icon for daily question. Have daily question pop up automatically when available (with setting to toggle). Idle rewards as well (remove icon) → DONE (2026-09-11): both header icons are now fallback-only. Two new settings (both ON by default, settings merge supplies them — no migration): `autoDailyEquation` auto-starts today's equation (the existing daily-equation mode, start toast included) at most once per local dayKey while unsolved, and `autoDailyBonus` auto-claims a claimable streak bonus once per dayKey through the same grant path/toast (`claim` is internally claimable-guarded, so double-fire can't double-pay). While either toggle is OFF, the 📅/🎁 header button renders exactly as before, so every state has an entry point. Both effects are onboarding-gated like the gem pocket (the tutorial owns the display first); per-dayKey ref guards make repeat renders no-ops. i18n en/es + SettingsPanel rows; useDailyBonus now exposes `dayKey` for the per-day guards.
- [x] gems should be more rare and buying gems with minerals should increase in price after each purchase → DONE (2026-09-11): the direct mineral→gem purchase is now a self-limiting fallback, not a faucet: base 100,000 minerals, each subsequent LIFETIME purchase costs ×1.10 (`GEM_PURCHASE_ESCALATION`, save field `gemsBoughtWithMinerals` — survives prestige, save version 12 migration; pb_hooks save-schema sync pin bumped in step). Price computed on the pure module (`getGemPurchaseCost`), surfaced on the buy button + settings preview, free-path sim uses the same curve. Consequence, benchmark recalibrated: no positive escalation can keep the old 30-day full-collection benchmark (it needs ~400 flat-cost mints), so the F2P horizon moved 30→45 days (deterministic crossover between day 42 and 45; 1634/1782 gems vs 1675 cost). Guardrail 1 wording preserved: free players reach the same end-state, only slower — and a future change pushing the crossover past 45 days fails the benchmark on purpose.
- [x] background canvas should cover whole screen and be more random (less patterns) → DONE (2026-09-11): CaveBackground reworked to a full-screen 4-layer background at the MinesOfDoom root (flat tint wash + far row layer at 0.5× + mid rows at 1× + jagged rock wall columns on both screen edges at 1.25× with random-cut inner edges, ore flecks and crystals); the mining canvas box is transparent now; caveTiles.ts gained the wall-strip baker (buildCaveWall / caveWallUri / caveWallWidthPx) + tests.
- [x] since they give minerals, mineral pocket instead of gem pocket —
      art is now the mineral chunk (emoji + pixel-art); toasts/a11y say
      "mineral pocket". `testID="gem-pocket"` and module names kept for
      e2e/test stability.
- [ ] finish the `packSkin` release — the in-repo half LANDED (custom-skin one-time purchase, catalog row `packSkin`, web + native pickers, grant path, i18n; Stripe test product/price and the `pack_skin` Play Billing SKU are live in test mode; all gates green — full writeup in git history). Remaining: (4) manual ad-hoc pass on a device (human).
- [ ] continuous task (do not complete): document feature map and fill out feature gaps and potential product improvements. Do not implement any feature gaps until greenlit (nothing in `docs/gap-ranking.md` is greenlit).
  - **Sweep status (passes 33–70, 2026-09-10 → 2026-09-12):** 27 architecture layers audited end to end (account/session, content authoring, release pipeline, web discoverability, numeric ledger, dependencies, server-side verification, tick scheduling, external code, test layer, asset generation, React state, compliance, storage, listener lifecycle, fault handling, concurrency, server API surface, juice/game feel, onboarding, input, settings, rewarded-ad economy, cosmetics rendering, IAP queue, web-e2e stub surface, message/toast surface). Everything that LANDED from the sweep — Tier 1 real bugs #24–#27, the onboarding save-code seams, the erase-all one-off, the empty-submit guard, the settings-hook net, the stale ad-offer clears, the hold-to-mine i18n, the Stripe/GSI loader hygiene — is in git history together with its pass-by-pass writeups; only the open set is tracked here.
  - **Still open (all ranked in `docs/gap-ranking.md`):** Tier 1 #22 `account:web-erasure` (feature gap), #28 `compliance:pb-portal-backdoor` (needs the owner's remove-vs-gate call); plus the pending-greenlit Tier 2/3 findings from the swept layers and the human / trigger-gated items (F36.1 Search Console + AdSense account approval; Tier 1 #14 `web:pwa` on its web-growth trigger).

- `docs/blockers.md` — the IAP external remnants (iOS `APPLE_*` App Store Connect
  API key for the sidecar; the Stripe `sk_live` flip at launch) and the three
  OS-level VPS hardening steps that need a human at the terminal.
- `docs/security-audit.md` — the S4/S6 manual pre-production checklist (store
  listing privacy/terms links, the 13+ questionnaire steps, the AdMob
  console "Not child-directed" setting, the post-launch S6 revisit trigger).
- `docs/backlog.md` — the intentionally deferred iOS track (AdMob iOS entry,
  App Store IAP products + credentials, iOS on-device verification passes).
- `docs/features.md` — the feature map (what exists, per section).
- `docs/gap-ranking.md` — every open gap, ranked by impact (the former
  features.md §7, moved 2026-09), with trigger-gated candidates
(including `offline:clock-hwm`, deliberately low priority per its own
sources, and `share:clipboard-opt-in`). Reopen on the signals
recorded there (first production release + the guardrail-5 signal
batch, endgame-lifetime content pass, es-market,
share-badge/cosmetics demand).
