# Metro (Expo), jest's moduleNameMapper, and the "jest in dependencies" setup all
# assume a flat, npm-like node_modules. pnpm's default isolated layout breaks
# Metro's resolution, so use the hoisted layout instead.
node-linker=hoisted
